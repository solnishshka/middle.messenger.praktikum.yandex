{
  "name": "middle.messenger.praktikum.yandex",
  "version": "1.0.0",
  "description": "web messenger",
  "scripts": {
    "build": "rimraf dist && parcel build src/pages/index/index.hbs --no-cache && cp -r static dist",
    "dev": "cp -r static dist/static && PORT=3000 parcel src/pages/index/index.hbs --no-cache",
    "start": "npm run build && node app"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/solnishshka/middle.messenger.praktikum.yandex.git"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@anikin/parcel-transformer-handlebars": "1.0.9",
    "@parcel/transformer-sass": "2.6.0",
    "parcel": "2.6.0",
    "parcel-transformer-handlebars": "1.0.0",
    "rimraf": "3.0.2",
    "sass": "1.52.1"
  },
  "staticFiles": {
    "staticPath": "static"
  },
  "dependencies": {
    "express": "4.18.1"
  }
}
